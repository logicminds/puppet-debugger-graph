source 'https://rubygems.org'

# Specify your gem's dependencies in debugger-graph.gemspec
gem 'ruby-graphviz'

group :dev, :test do
  gem 'puppet-debugger', '>= 0.7.0'
  gem 'pry'
  gem 'CFPropertyList'
  gem 'rake'
  gem 'rspec', '>= 3.6'
  gem 'debugger-graph', path: './'
end
